@using gestion_de_notes.Models.Enum
@model gestion_de_notes.Models.Classe

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Classe</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdClasse" />
            <div class="form-group">
                <label asp-for="Niveau" class="control-label"></label>
                <select asp-for="Niveau" class="form-control" asp-items="Html.GetEnumSelectList<NiveauEnum>()"></select>
                <span asp-validation-for="Niveau" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Groupe" class="control-label"></label>
                <select asp-for="Groupe" class="form-control" asp-items="Html.GetEnumSelectList<GroupeEnum>()"></select>
                <span asp-validation-for="Groupe" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
