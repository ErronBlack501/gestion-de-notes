@model IEnumerable<gestion_de_notes.Models.Groupe>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Design)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Design)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdGroupe">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdGroupe">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdGroupe">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
