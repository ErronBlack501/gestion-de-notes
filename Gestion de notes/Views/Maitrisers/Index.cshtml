@model IEnumerable<gestion_de_notes.Models.Maitriser>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Professeur)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Matiere)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Professeur.NomPrenom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Matiere.NomMatiere)
            </td>
            <td>
                @Html.ActionLink("Details", "Details", new { id = item.ProfesseurId, mid = item.MatiereId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ProfesseurId, mid = item.MatiereId })
            </td>
        </tr>
}
    </tbody>
</table>
